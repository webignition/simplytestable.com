imports:
    - { resource: config_dev.yml }
    - { resource: services_test.yml }
    - { resource: "@SimplyTestableWebsiteBundle/Resources/config/parameters_test.yml" }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

parameters:
    database_driver: pdo_mysql
    database_host: localhost
    database_port: null
    database_name: test_simplytestable_com
    database_user: root
    database_password: root
