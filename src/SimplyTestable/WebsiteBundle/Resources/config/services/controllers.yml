services:
  SimplyTestable\WebsiteBundle\Controller\ExceptionController:
    class: SimplyTestable\WebsiteBundle\Controller\ExceptionController
    public: true
    arguments:
        $debug: '%kernel.debug%'
        $twig: '@twig'
        $notFoundRedirectService: '@SimplyTestable\WebsiteBundle\Services\NotFoundRedirectService'
        $mailer: "@mailer"
        $testimonialService: '@SimplyTestable\WebsiteBundle\Services\TestimonialService'
