<p class="heading hidden-md hidden-lg">{{ name }}</p>                                     

<ul class="item-list">
    <li>
        {% include 'SimplyTestableWebsiteBundle:Partials:boolean-icon.html.twig'
            with {
                'is_set': discovery_via_sitemap.value,
                'name': discovery_via_sitemap.name.primary
            }
        %}
    </li>                                        
    <li>
        {% include 'SimplyTestableWebsiteBundle:Partials:boolean-icon.html.twig'
            with {
                'is_set': discovery_via_crawl.value,
                'name': discovery_via_crawl.name.primary
            }
        %}
    </li>
</ul>  